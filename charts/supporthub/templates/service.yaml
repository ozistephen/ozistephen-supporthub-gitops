apiVersion: v1
kind: Service
metadata:
  name: {{ include "supporthub.fullname" . }}
  labels:
    {{- include "supporthub.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "supporthub.selectorLabels" . | nindent 4 }}
